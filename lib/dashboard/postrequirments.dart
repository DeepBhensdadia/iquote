import 'package:flutter/material.dart';

class PostRequirments extends StatefulWidget {
  const PostRequirments({super.key});

  @override
  State<PostRequirments> createState() => _PostRequirmentsState();
}

class _PostRequirmentsState extends State<PostRequirments> {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        // appBar: AppBar(title: const Text("HomeScreen")),
        body: Column(
          children: const [


          ],
        ),
      ),
    );
  }
}
